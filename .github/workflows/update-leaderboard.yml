name: อัปเดต Leaderboard ทุกชั่วโมง

on:
  schedule:
    - cron: '0 * * * *'  # ทำงานทุกชั่วโมงที่นาที 0 (UTC)
  workflow_dispatch:     # สามารถ trigger ด้วยตัวเองได้ด้วย

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true  # เพื่อให้สามารถ push ข้อมูลกลับไปใน repository ได้

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'         # เปลี่ยนเป็นเวอร์ชัน Node.js ที่คุณต้องการ

      - name: Install Dependencies
        run: npm install

      - name: Run Leaderboard Update Script
        run: npm run update-leaderboard

      - name: Commit leaderboard changes if any
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet public/leaderboard.json; then
            echo "พบการเปลี่ยนแปลงใน public/leaderboard.json"
            git add public/leaderboard.json
            git commit -m "📊 อัปเดตข้อมูล leaderboard [skip ci]"
            git push origin main
          else
            echo "ไม่มีการเปลี่ยนแปลงใน public/leaderboard.json"
          fi
